# This program generates a file full of characters between 0 and 255. This file can then
# be used to discover the characters in a font using NotePad.

procedure main()

  file02 := open("AllChars.txt","w")
  every i := 0 to 255 do {
    write(file02,i," ",char(i))
  }
end
